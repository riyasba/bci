class TimeSlotDays {
    String weekdays;
    DateTime fromDate;
    DateTime toDate;

    TimeSlotDays({
        required this.weekdays,
        required this.fromDate,
        required this.toDate,
    });
}